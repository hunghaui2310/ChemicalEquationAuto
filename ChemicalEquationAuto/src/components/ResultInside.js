import React, { useState } from "react";
import { SafeAreaView, StyleSheet, TextInput, View, Text } from "react-native";

const ResultInside = () => {

    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
};

const styles = StyleSheet.create({

});

export default ResultInside;